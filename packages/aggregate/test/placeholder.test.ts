import { describe, expect, it } from '@jest/globals';

describe('aggregate package placeholder', () => {
  it('keeps package test task green when no package-local tests exist', () => {
    expect(true).toBe(true);
  });
});
